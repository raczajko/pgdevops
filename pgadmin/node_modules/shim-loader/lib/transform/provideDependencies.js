'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = provideDependencies;
function provideDependencies(deps) {

  if (deps.length) {
    var code = deps.map(function (dep) {
      var values = dep.split(':');
      var alias = values[0];

      var statement = 'require(\'' + alias + '\');';

      if (values.length === 2) {
        var name = values[1];
        return 'var ' + name + ' = global.' + name + ' = ' + statement;
      }

      return statement;
    }).join('\n');

    return code;
  }
  return '';
}