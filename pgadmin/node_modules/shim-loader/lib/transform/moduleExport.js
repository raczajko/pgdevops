'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = moduleExport;

var _constant = require('./constant');

function moduleExport(mod) {
  if (Array.isArray(mod)) {

    var body = mod.map(function (name) {
      return '  ' + name + ': typeof ' + name + ' !== "undefined" ? ' + name + ' : global.' + name;
    }).join(',\n');

    return [_constant.shimModuleExport + '({', body, '});'].join('\n');
  }
  return _constant.shimModuleExport + '(typeof ' + mod + ' !== "undefined" ? ' + mod + ' : global.' + mod + ');';
}